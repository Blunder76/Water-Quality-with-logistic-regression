# -*- coding: utf-8 -*-
"""Water Quality Prediction Logistic Regression.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1yJWobOqc_PCE3gSwh8Kq64ehiAyQdkLI
"""

!pip install -q kaggle

from google.colab import files
files.upload()

! mkdir ~/.kaggle
! cp kaggle.json ~/.kaggle/
! chmod 600 ~/.kaggle/kaggle.json

! kaggle datasets download -d parnianmalekian/water-quality

!unzip water-quality.zip

import pandas as pd

df = pd.read_csv('water_potability.csv')

df.head(5)

df.info()

df.describe()

df.Potability.unique()

df.isna().sum()

import matplotlib.pyplot as plt
import seaborn as sns
plt.figure(figsize=(6, 6))
heatmap = sns.heatmap(df.corr()[['Potability']].sort_values(by='Potability', ascending=False), vmin=-1, vmax=1, annot=True)
heatmap.set_title('Feature Potability Correlation', fontdict={'fontsize':18}, pad=16)

df.fillna(df.mean(), inplace = True) #filling in the missing values
df

df.isnull().sum()

#import seaborn as sns
sns.pairplot(data = df)

#df cols
X = df.drop('Potability', axis=1)
y = df['Potability'] #Target variable

x.shape, y.shape

from sklearn.model_selection import train_test_split
x_train, x_test, y_train, y_test = train_test_split(x, y, test_size=0.33, random_state=42)

from sklearn.linear_model import LogisticRegression
from sklearn.metrics import confusion_matrix, accuracy_score, classification_report

model = LogisticRegression(max_iter=120,random_state=0, n_jobs=20)

model.fit(x_train, y_train)

pred_lg = model.predict(x_test)

lg = accuracy_score(y_test, pred_lg)
print(lg)

print(classification_report(y_test,pred_lg))

